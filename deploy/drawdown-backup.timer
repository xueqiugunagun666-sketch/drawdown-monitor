[Unit]
Description=定期备份数据库

[Timer]
# 每天低峰一次。真正不可再生的是账号、钱包备注与报警状态；行情历史可回填。
OnCalendar=*-*-* 04:00:00
# 错过低峰窗口就等下一天，不能在白天重启后立刻补跑 2GB 重备份。
Persistent=false
RandomizedDelaySec=1800
AccuracySec=5min

[Install]
WantedBy=timers.target
